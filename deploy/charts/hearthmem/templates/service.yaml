apiVersion: v1
kind: Service
metadata:
  name: {{ include "hearthmem.fullname" . }}
  labels:
    {{- include "hearthmem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hearthmem.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
