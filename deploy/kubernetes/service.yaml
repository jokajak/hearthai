apiVersion: v1
kind: Service
metadata:
  name: hearthmem
  labels:
    app.kubernetes.io/name: hearthmem
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hearthmem
  ports:
    - name: http
      port: 80
      targetPort: http
