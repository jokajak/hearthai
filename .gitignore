__pycache__/
*.py[cod]
.pytest_cache/
data/
.worktrees/
*.egg-info/
.venv/

# Built from the local chart dependency; never commit packaged binaries.
deploy/charts/hearthai/charts/

# Rust build output.
target/

# uv writes these when a test run resolves pytest; the Python packages here have
# no runtime dependencies, so there is nothing worth locking.
uv.lock
